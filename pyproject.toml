[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ratchet"
version = "0.1.0"
description = "Structured, autonomous AI-driven software development orchestration"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
    "psycopg[async]>=3.1",
    "psycopg-pool>=3.1",
    "alembic>=1.13",
    "pyyaml>=6.0",
    "fastapi>=0.111",
    "uvicorn[standard]>=0.29",
    "jinja2>=3.1",
    "python-multipart>=0.0.9",
    "itsdangerous>=2.1",
    "sse-starlette>=1.6",
    "websockets>=12.0",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.4",
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "mypy>=1.0",
    "httpx>=0.27",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["core*", "worker*", "tui*", "web*", "orchestrator*", "remote_worker*"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
ignore = []

[tool.pytest.ini_options]
asyncio_mode = "auto"

[tool.mypy]
strict = true
python_version = "3.12"
exclude = ["core/tests/", "worker/tests/", "web/tests/", "orchestrator/tests/", "remote_worker/tests/"]
python_executable = ".venv/bin/python"

[[tool.mypy.overrides]]
module = ["psycopg.*", "psycopg_pool.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["alembic.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["yaml"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["websockets.*"]
ignore_missing_imports = true
